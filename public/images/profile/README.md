# Optional profile image

Add an optimized professional photo as `profile.webp`, then set
`siteConfig.profileImage.enabled` to `true` in `src/data/content.ts`.

Recommended crop: portrait, 4:5 ratio, at least 720 × 900 pixels.
